---
doc_type: "table_knowledge"
source: "feishu_bitable"
generated_at: "2026-05-21T01:15:16+08:00"
table_name: "aidata2025.dwd.dwd_oper_delivery_adjust_user_di"
table_cn_name: "adjust数据归因明细表-ai产品"
metadata_status: "complete"
field_status: "available"
projects: ["ai"]
project_dirs: ["ai"]
warehouse_space: "aidata"
layer: "DWD明细层"
subject: "增长主题"
secondary_subject: "adjust归因"
table_type: "事实表"
grain: ""
primary_key: ""
update_frequency: "T+1 9:00"
owner: "恬静"
priority: "低"
core_table: "否"
query_cost: "低"
source_record_ids: ["recvh3C8YGKnSZ"]
field_count: "23"
---

# 表名：aidata2025.dwd.dwd_oper_delivery_adjust_user_di

> 表中文名：adjust数据归因明细表-ai产品
> 生成来源：飞书 Bitable「数据知识库」；状态：`complete` / `available`。

## 1. 表定位
项目：ai；分层：DWD明细层；表类型：事实表；主题：增长主题；二级主题：adjust归因。

## 2. 基础信息
| 属性 | 值 |
|---|---|
| 表中文名 | adjust数据归因明细表-ai产品 |
| 项目 | ai |
| 数仓空间 | aidata |
| 分层 | DWD明细层 |
| 主题 | 增长主题 |
| 二级主题 | adjust归因 |
| 表类型 | 事实表 |
| 行粒度 | - |
| 主键字段 | - |
| 核心表 | 否 |
| 更新频率 | T+1 9:00 |
| 维护人 | 恬静 |
| 使用优先级 | 低 |
| 查询成本 | 低 |
| 数据来源 | adjust |

## 3. 字段字典
| 字段名 | 类型 | 字段角色 | 是否核心字段 | 说明 |
|---|---|---|---|---|
| `event_date` | date | 时间 | 是 | 日期 |
| `package_name` | string | 主键 | 是 | 包名 |
| `app_name` | string | 维度 | 是 | 产品名 |
| `event_name` | string | 维度 | 是 | 事件名/事件英文名 |
| `activity_kind` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `created_at` | string | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `created_time` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `installed_at` | string | 时间 | 是 | 安装时间 |
| `installed_time` | timestamp | 时间 | 是 | 安装时间 |
| `platform` | string | 维度 | 是 | 应用所属平台，如 iOS、Android、Web。 |
| `country` | string | 维度 | 是 | 国家 |
| `adid` | string | 维度 | 否 | 用户的广告id身份，也是一种唯一id |
| `campaign_id` | string | 主键 | 是 | 投放campaign id |
| `campaign_name` | string | 维度 | 否 | 投放campaign名称 |
| `adgroup_id` | string | 主键 | 是 | 广告组id |
| `adgroup_name` | string | 维度 | 否 | 投放广告组 |
| `tracker_name` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `revenue_usd` | float | 指标 | 是 | 收入 |
| `product_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `currency` | string | 维度 | 否 | 汇率 |
| `gclid` | string | 维度 | 否 | 一种用户的身份id |
| `network_type` | string | 维度 | 否 | 设备网络类型 |
| `engagement_type` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |

## 4. 核心字段
| 字段名 | 类型 | 字段角色 | 说明 |
|---|---|---|---|
| `event_date` | date | 时间 | 日期 |
| `package_name` | string | 主键 | 包名 |
| `app_name` | string | 维度 | 产品名 |
| `event_name` | string | 维度 | 事件名/事件英文名 |
| `created_at` | string | 时间 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `created_time` | timestamp | 时间 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `installed_at` | string | 时间 | 安装时间 |
| `installed_time` | timestamp | 时间 | 安装时间 |
| `platform` | string | 维度 | 应用所属平台，如 iOS、Android、Web。 |
| `country` | string | 维度 | 国家 |
| `campaign_id` | string | 主键 | 投放campaign id |
| `adgroup_id` | string | 主键 | 广告组id |
| `revenue_usd` | float | 指标 | 收入 |
| `product_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |

## 5. 典型使用场景
- 用于追溯用户、订单、事件等明细事实，适合做深度诊断或二次聚合。

## 6. 查询风险与注意事项
- 存在 `event_date` 字段，按天查询时建议显式添加日期范围，避免全表扫描。

## 7. 血缘和关联
| 项目 | 内容 |
|---|---|
| 上层表 | fx-editor.analytics_263446298.daily_adjust_report_*， ods.ods_adjust_collart_android_1d， |
| 是否需要展示血缘 | 否 |
| 数据来源 | adjust |
| 数据表地址 | [BigQuery](https://console.cloud.google.com/bigquery?project=aidata2025&ws=!1m5!1m4!4m3!1saidata2025!2sdwd!3sdwd_oper_delivery_adjust_user_di) |
| 任务地址 | [任务](https://console.cloud.google.com/bigquery/dataform/locations/us-east1/repositories/aidata/workspaces/script/files/definitions%2Fdwd%2Fdaily%2Fh_dwd_oper_delivery_adjust_user_di.sqlx?project=aidata2025) |
| 看板 | - |

## 8. 待补充项
- 补充 `行粒度`。
- 补充 `主键字段`。
