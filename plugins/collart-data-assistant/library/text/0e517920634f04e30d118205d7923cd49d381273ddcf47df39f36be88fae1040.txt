---
doc_type: "table_knowledge"
source: "feishu_bitable"
generated_at: "2026-05-21T01:15:16+08:00"
table_name: "aidata2025.dwd.dwd_oper_user_basic_di"
table_cn_name: "ai组每日活跃用户id表"
metadata_status: "complete"
field_status: "available"
projects: ["ai"]
project_dirs: ["ai"]
warehouse_space: "aidata"
layer: "DWD明细层"
subject: "firebase埋点通用"
secondary_subject: "活跃用户属性"
table_type: "事实表"
grain: "每天每个user_pseudo_Id一行"
primary_key: "event_Date,user_pseudo_id"
update_frequency: "T+1 8:00"
owner: "恬静"
priority: "低"
core_table: "否"
query_cost: "低"
source_record_ids: ["recvh3C8YGyKzx"]
field_count: "29"
---

# 表名：aidata2025.dwd.dwd_oper_user_basic_di

> 表中文名：ai组每日活跃用户id表
> 生成来源：飞书 Bitable「数据知识库」；状态：`complete` / `available`。

## 1. 表定位
项目：ai；分层：DWD明细层；表类型：事实表；主题：firebase埋点通用；二级主题：活跃用户属性；行粒度：每天每个user_pseudo_Id一行。

## 2. 基础信息
| 属性 | 值 |
|---|---|
| 表中文名 | ai组每日活跃用户id表 |
| 项目 | ai |
| 数仓空间 | aidata |
| 分层 | DWD明细层 |
| 主题 | firebase埋点通用 |
| 二级主题 | 活跃用户属性 |
| 表类型 | 事实表 |
| 行粒度 | 每天每个user_pseudo_Id一行 |
| 主键字段 | event_Date,user_pseudo_id |
| 核心表 | 否 |
| 更新频率 | T+1 8:00 |
| 维护人 | 恬静 |
| 使用优先级 | 低 |
| 查询成本 | 低 |
| 数据来源 | 客户端埋点 |

## 3. 字段字典
| 字段名 | 类型 | 字段角色 | 是否核心字段 | 说明 |
|---|---|---|---|---|
| `event_date` | date | 时间 | 是 | 日期 |
| `app_name` | string | 维度 | 是 | 产品名 |
| `user_pseudo_id` | string | 主键 | 是 | 用户id，firebase分配的唯一id |
| `user_id` | string | 主键 | 是 | 用户id，内部平台分配的唯一id |
| `adjust_id` | string | 主键 | 是 | adjust id |
| `atlasv_uid` | string | 维度 | 否 | 技术团队给用户定义的唯一id |
| `package_name` | string | 主键 | 是 | 包名 |
| `traffic_src_type` | string | 维度 | 否 | 投放渠道类型 |
| `traffic_src_name` | string | 维度 | 否 | 投放渠道名称 |
| `country` | string | 维度 | 是 | 国家 |
| `country_level` | int | 指标 | 是 | 国家T级；比如T1/T2/T3 |
| `city` | string | 维度 | 否 | 用户所在城市 |
| `device_language` | string | 维度 | 否 | 设备语言 |
| `device_total_ram` | string | 维度 | 否 | 设备内存 |
| `mobile_brand_name` | string | 维度 | 否 | 设备品牌名 |
| `mobile_model_name` | string | 维度 | 否 | 设备型号 |
| `app_version` | string | 维度 | 否 | app客户端版本号 |
| `network_type` | string | 维度 | 否 | 设备网络类型 |
| `is_new` | boolean | 维度 | 否 | 是否新老用户，true代表新用户、false代表老用户 |
| `is_active` | boolean | 维度 | 否 | 布尔标记字段，用于表示是/否状态。 |
| `register_time` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `mobile_marketing_name` | string | 维度 | 否 | 设备型号 |
| `vip_type` | string | 维度 | 否 | 是否VIP |
| `subscribe_time` | timestamp | 时间 | 是 | 订阅时间 |
| `subscribe_product_id` | string | 主键 | 是 | 订阅商品 |
| `os_classify` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `os_cpu_info` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `subscriptions` | record | 维度 | 否 | 复杂结构字段，通常包含嵌套对象或数组信息。 |
| `advertising_id` | string | 主键 | 是 | 用户的广告id身份，也是一种唯一id |

## 4. 核心字段
| 字段名 | 类型 | 字段角色 | 说明 |
|---|---|---|---|
| `event_date` | date | 时间 | 日期 |
| `app_name` | string | 维度 | 产品名 |
| `user_pseudo_id` | string | 主键 | 用户id，firebase分配的唯一id |
| `user_id` | string | 主键 | 用户id，内部平台分配的唯一id |
| `adjust_id` | string | 主键 | adjust id |
| `package_name` | string | 主键 | 包名 |
| `country` | string | 维度 | 国家 |
| `country_level` | int | 指标 | 国家T级；比如T1/T2/T3 |
| `register_time` | timestamp | 时间 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `subscribe_time` | timestamp | 时间 | 订阅时间 |
| `subscribe_product_id` | string | 主键 | 订阅商品 |
| `advertising_id` | string | 主键 | 用户的广告id身份，也是一种唯一id |

## 5. 典型使用场景
- 用于追溯用户、订单、事件等明细事实，适合做深度诊断或二次聚合。

## 6. 查询风险与注意事项
- 存在 `event_date` 字段，按天查询时建议显式添加日期范围，避免全表扫描。

## 7. 血缘和关联
| 项目 | 内容 |
|---|---|
| 上层表 | aidata2025.dwd.dwd_oper_user_event_di |
| 是否需要展示血缘 | 是 |
| 数据来源 | 客户端埋点 |
| 数据表地址 | [BigQuery](https://console.cloud.google.com/bigquery?project=aidata2025&ws=!1m5!1m4!4m3!1saidata2025!2sdwd!3sdwd_oper_user_basic_di) |
| 任务地址 | [任务](https://console.cloud.google.com/bigquery/dataform/locations/us-east1/repositories/aidata/workspaces/script/files/definitions%2Fdwd%2Fdaily%2Fh_dwd_oper_user_basic_di.sqlx?project=aidata2025) |
| 看板 | - |

## 8. 待补充项
- 暂无，后续可补充典型 SQL 和业务口径案例。
