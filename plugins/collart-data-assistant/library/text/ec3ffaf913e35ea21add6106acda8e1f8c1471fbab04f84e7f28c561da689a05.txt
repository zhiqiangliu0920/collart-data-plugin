# aidata2025.ads_collart.ads_oper_user_new_collart_di · Collart ios/android每日新增用户id表

> 业务状态：historical；复核：reviewed_static。以下完整字段定义来自 2026-05-21 导出，未经本次生产验证。

## 2026-05-21 字典依据

来源：飞书多维表格导出；生成时间 `2026-05-21T01:15:16+08:00`；记录标识 `recvh3C8YGI1yN`。这是原始元数据日期，不是当前生效日期。

- 原产品标记：['collart android']
- 原登记粒度：每个user_pseudo_Id一行
- 原登记主键：user_pseudo_Id
- 更新频率：T+1 8:00
- 原登记层级：ADS应用层
- 主题：firebase埋点通用
- 子主题：用户属性
- 负责人：志强

### 字段字典（原日期）

| 字段名 | 类型 | 字段角色 | 是否核心字段 | 说明 |
|---|---|---|---|---|
| `event_date` | date | 时间 | 是 | 日期 |
| `app_name` | string | 维度 | 是 | 产品名 |
| `user_pseudo_id` | string | 主键 | 是 | 用户id，firebase分配的唯一id |
| `user_Id` | string | 主键 | 是 | 用户id，内部平台分配的唯一id |
| `package_name` | string | 主键 | 是 | 包名 |
| `traffic_src_type` | string | 维度 | 否 | 投放渠道类型 |
| `traffic_src_name` | string | 维度 | 否 | 投放渠道名称 |
| `country` | string | 维度 | 是 | 国家 |
| `country_level` | int | 指标 | 是 | 国家T级；比如T1/T2/T3 |
| `city` | string | 维度 | 否 | 用户所在城市 |
| `device_language` | string | 维度 | 否 | 设备语言 |
| `device_total_ram` | string | 维度 | 否 | 设备内存 |
| `mobile_brand_name` | string | 维度 | 否 | 设备品牌名 |
| `mobile_model_name` | string | 维度 | 否 | 设备型号 |
| `app_version` | string | 维度 | 否 | app客户端版本号 |
| `network_type` | string | 维度 | 否 | 设备网络类型 |
| `register_time` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `atlasv_uid` | string | 维度 | 否 | 技术团队给用户定义的唯一id |
| `mobile_marketing_name` | string | 维度 | 否 | 设备型号 |
| `is_vip` | boolean | 维度 | 否 | 是否新老用户，true代表付费用户、false代表免费用户 |
| `is_remove` | boolean | 维度 | 否 | 是否卸载用户，true代表新用户、false代表老用户 |
| `is_purchase` | boolean | 维度 | 否 | 是否新老用户，true代表新用户、false代表老用户 |
| `subscribe_time` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `subscribe_product_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `device_id` | string | 主键 | 是 | 设备id |
| `advertising_id` | string | 主键 | 是 | 用户的广告id身份，也是一种唯一id |
| `is_retain_2` | int | 指标 | 否 | 次日留存用户数，计算新用户次留需要过滤is_new=true时用retain_2/dau计算； |
| `is_retain_3` | int | 指标 | 否 | 该字段表示用户的3日留存情况，即某一特定时间节点新增的用户在3天后仍然活跃的数量，数据类型为整数。 |
| `is_retain_4` | int | 指标 | 否 | 该字段表示用户的4日留存情况，即某一特定时间节点新增的用户在4天后仍然活跃的数量，数据类型为整数。 |
| `is_retain_5` | int | 指标 | 否 | 该字段表示用户的5日留存情况，即某一特定时间节点新增的用户在5天后仍然活跃的数量，数据类型为整数。 |
| `is_retain_6` | int | 指标 | 否 | 该字段表示用户的6日留存情况，即某一特定时间节点新增的用户在6天后仍然活跃的数量，数据类型为整数。 |
| `is_retain_7` | int | 指标 | 否 | 该字段表示用户的7日留存情况，即某一特定时间节点新增的用户在7天后仍然活跃的数量，数据类型为整数。 |
| `is_retain_14` | int | 指标 | 否 | 该字段表示用户的14日留存情况，即某一特定时间节点新增的用户在14天后仍然活跃的数量，数据类型为整数。 |
| `is_retain_21` | int | 指标 | 否 | 该字段表示用户的21日留存情况，即某一特定时间节点新增的用户在21天后仍然活跃的数量，数据类型为整数。 |
| `is_retain_30` | int | 指标 | 否 | 该字段表示用户的30日留存情况，即某一特定时间节点新增的用户在30天后仍然活跃的数量，数据类型为整数。 |
| `is_rolling_retain_2` | int | 指标 | 否 | 滚动留存指标 |
| `is_rolling_retain_3` | int | 指标 | 否 | 滚动留存指标 |
| `is_rolling_retain_4` | int | 指标 | 否 | 滚动留存指标 |
| `is_rolling_retain_5` | int | 指标 | 否 | 滚动留存指标 |
| `is_rolling_retain_6` | int | 指标 | 否 | 滚动留存指标 |
| `is_rolling_retain_7` | int | 指标 | 否 | 滚动留存指标 |
| `is_rolling_retain_14` | int | 指标 | 否 | 滚动留存指标 |
| `is_rolling_retain_21` | int | 指标 | 否 | 滚动留存指标 |
| `is_rolling_retain_30` | int | 指标 | 否 | 滚动留存指标 |

### 上游与来源

| 项目 | 内容 |
|---|---|
| 上层表 | aidata2025.dwd.dwd_oper_user_collart_di |
| 是否需要展示血缘 | 是 |
| 数据来源 | 客户端埋点 |
| 数据表地址 | [BigQuery](https://console.cloud.google.com/bigquery?project=aidata2025&ws=!1m5!1m4!4m3!1saidata2025!2sads_collart!3sads_oper_user_new_collart_di) |
| 任务地址 | [任务](https://console.cloud.google.com/bigquery/dataform/locations/us-east1/repositories/aidata/workspaces/script/files/definitions%2Fads%2Fcollart%2Fads_oper_user_new_collart_di.sqlx?project=aidata2025) |
| 看板 | - |

### 使用限制

- retain_2 的历史说明为次日；其他 retain_N 的自动解释可能偏移一天，不能按字段后缀直接认定 Dn。
