# dwd_oper_user_collart_di

## 表信息

| 属性 | 值 |
|:---|:---|
| 项目 | aidata2025 |
| 库 | dwd |
| 表 | dwd_oper_user_collart_di |
| 更新频率 | 日更 (DI - Daily Incremental) |
| 业务域 | Android 用户运营数据 |

## 核心字段

| 字段名 | 类型 | 说明 |
|:---|:---|:---|
| event_date | DATE | 事件日期 |
| user_pseudo_id | STRING | 用户伪ID（去重标识） |
| is_new | STRING | 新老用户标识 |
| country | STRING | 国家/地区 |
| package_name | STRING | 应用包名 |

## 常用过滤条件

- `package_name = 'free.ai.photo.generator.collart.ai'` - Collart Android 端
- `event_date >= DATE_SUB(CURRENT_DATE(), INTERVAL 30 DAY)` - 近30天

## 典型用法

### 获取 DAU 和新老用户分布
```sql
SELECT
  event_date,
  is_new,
  COUNT(DISTINCT user_pseudo_id) AS dau
FROM aidata2025.dwd.dwd_oper_user_collart_di
WHERE event_date BETWEEN DATE_SUB(CURRENT_DATE(), INTERVAL 7 DAY) AND DATE_SUB(CURRENT_DATE(), INTERVAL 1 DAY)
  AND package_name = 'free.ai.photo.generator.collart.ai'
GROUP BY event_date, is_new
ORDER BY event_date DESC
```

### 与 Firebase 事件关联（用户行为分析）
```sql
WITH feature_clicks AS (
  SELECT
    PARSE_DATE('%Y%m%d', event_date) AS event_date,
    user_pseudo_id,
    event_name,
    (SELECT value.string_value FROM UNNEST(event_params) WHERE key = 'feature') AS feature
  FROM `storytemplate-10a27.analytics_232977577.events_*`
  WHERE _TABLE_SUFFIX BETWEEN FORMAT_DATE('%Y%m%d', DATE_SUB(CURRENT_DATE(), INTERVAL 7 DAY))
    AND FORMAT_DATE('%Y%m%d', DATE_SUB(CURRENT_DATE(), INTERVAL 1 DAY))
    AND app_info.id = 'free.ai.photo.generator.collart.ai'
    AND event_name = 'home_feature_click'
)

SELECT
  a.event_date,
  a.is_new,
  COUNT(DISTINCT a.user_pseudo_id) AS dau,
  COUNT(DISTINCT CASE WHEN fc.feature = 'aiVideo' THEN a.user_pseudo_id END) AS ai_video_uv
FROM aidata2025.dwd.dwd_oper_user_collart_di a
LEFT JOIN feature_clicks fc ON a.event_date = fc.event_date AND a.user_pseudo_id = fc.user_pseudo_id
WHERE a.event_date BETWEEN DATE_SUB(CURRENT_DATE(), INTERVAL 7 DAY) AND DATE_SUB(CURRENT_DATE(), INTERVAL 1 DAY)
  AND a.package_name = 'free.ai.photo.generator.collart.ai'
GROUP BY ALL
```

## 关联表

- `dwd_oper_user_collartweb_di` (Web端) - 可 UNION ALL 合并统计
- `storytemplate-10a27.analytics_232977577.events_*` (Firebase 原始事件) - 用于行为明细分析

## 更新记录

- 2026-04-09: 补充 is_new 字段说明，新增与 Firebase 事件关联示例

## 2026-05-21 字典依据

来源：飞书多维表格导出；生成时间 `2026-05-21T01:15:16+08:00`；记录标识 `recvh3C8YG6xpg`。这是原始元数据日期，不是当前生效日期。

- 原产品标记：['collart android']
- 原登记粒度：每天每个user_pseudo_Id一行
- 原登记主键：event_Date,user_pseudo_id
- 更新频率：T+1 8:00
- 原登记层级：DWD明细层
- 主题：firebase埋点通用
- 子主题：活跃用户属性
- 负责人：志强

### 字段字典（原日期）

| 字段名 | 类型 | 字段角色 | 是否核心字段 | 说明 |
|---|---|---|---|---|
| `event_date` | date | 时间 | 是 | 日期 |
| `app_name` | string | 维度 | 是 | 产品名 |
| `user_pseudo_id` | string | 主键 | 是 | 用户id，firebase分配的唯一id |
| `user_id` | string | 主键 | 是 | 用户id，内部平台分配的唯一id |
| `package_name` | string | 主键 | 是 | 包名 |
| `traffic_src_type` | string | 维度 | 否 | 投放渠道类型 |
| `traffic_src_name` | string | 维度 | 否 | 投放渠道名称 |
| `country` | string | 维度 | 是 | 国家 |
| `country_level` | int | 指标 | 是 | 国家T级；比如T1/T2/T3 |
| `city` | string | 维度 | 否 | 用户所在城市 |
| `device_language` | string | 维度 | 否 | 设备语言 |
| `device_total_ram` | string | 维度 | 否 | 设备内存 |
| `mobile_brand_name` | string | 维度 | 否 | 设备品牌名 |
| `mobile_model_name` | string | 维度 | 否 | 设备型号 |
| `app_version` | string | 维度 | 否 | app客户端版本号 |
| `network_type` | string | 维度 | 否 | 设备网络类型 |
| `is_new` | boolean | 维度 | 否 | 是否新老用户，true代表新用户、false代表老用户 |
| `register_time` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `atlasv_uid` | string | 维度 | 否 | 技术团队给用户定义的唯一id |
| `mobile_marketing_name` | string | 维度 | 否 | 设备型号 |
| `is_vip` | boolean | 维度 | 否 | 是否新老用户，true代表付费用户、false代表免费用户 |
| `is_remove` | boolean | 维度 | 否 | 是否卸载用户，true代表新用户、false代表老用户 |
| `is_purchase` | boolean | 维度 | 否 | 是否新老用户，true代表新用户、false代表老用户 |
| `subscribe_time` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `subscribe_product_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `device_id` | string | 主键 | 是 | 设备id |
| `advertising_id` | string | 主键 | 是 | 用户的广告id身份，也是一种唯一id |
| `is_active` | boolean | 维度 | 否 | 布尔标记字段，用于表示是/否状态。 |

### 上游与来源

| 项目 | 内容 |
|---|---|
| 上层表 | aidata2025.dwd.dwd_oper_user_basic_di |
| 是否需要展示血缘 | 是 |
| 数据来源 | 客户端埋点 |
| 数据表地址 | [BigQuery](https://console.cloud.google.com/bigquery?project=aidata2025&ws=!1m5!1m4!4m3!1saidata2025!2sdwd!3sdwd_oper_user_collart_di) |
| 任务地址 | [任务](https://console.cloud.google.com/bigquery/dataform/locations/us-east1/repositories/aidata/workspaces/script/files/definitions%2Fdwd%2Fdaily%2Fh_dwd_oper_user_collart_di.sqlx?project=aidata2025) |
| 看板 | - |

### 使用限制

- 仅有历史字典依据；当前表存在性、分区、写入状态与字段变更尚未重新验证。
