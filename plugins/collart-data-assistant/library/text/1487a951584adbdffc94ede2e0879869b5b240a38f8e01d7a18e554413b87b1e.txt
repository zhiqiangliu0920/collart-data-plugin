---
doc_type: "table_knowledge"
source: "feishu_bitable"
generated_at: "2026-05-21T01:15:16+08:00"
table_name: "aidata2025.dwd.dwd_cdct_woolong_request_di"
table_cn_name: "woolong表"
metadata_status: "complete"
field_status: "available"
projects: ["公司"]
project_dirs: ["1company"]
warehouse_space: "aidata"
layer: "DWD明细层"
subject: "收入成本主题"
secondary_subject: "服务端订阅数据"
table_type: "日志表"
grain: ""
primary_key: ""
update_frequency: ""
owner: "恬静"
priority: ""
core_table: "否"
query_cost: ""
source_record_ids: ["recvh8DhVv8Mh7"]
field_count: "20"
---

# 表名：aidata2025.dwd.dwd_cdct_woolong_request_di

> 表中文名：woolong表
> 生成来源：飞书 Bitable「数据知识库」；状态：`complete` / `available`。

## 1. 表定位
项目：公司；分层：DWD明细层；表类型：日志表；主题：收入成本主题；二级主题：服务端订阅数据。

## 2. 基础信息
| 属性 | 值 |
|---|---|
| 表中文名 | woolong表 |
| 项目 | 公司 |
| 数仓空间 | aidata |
| 分层 | DWD明细层 |
| 主题 | 收入成本主题 |
| 二级主题 | 服务端订阅数据 |
| 表类型 | 日志表 |
| 行粒度 | - |
| 主键字段 | - |
| 核心表 | 否 |
| 更新频率 | - |
| 维护人 | 恬静 |
| 使用优先级 | - |
| 查询成本 | - |
| 数据来源 | 服务端 |

## 3. 字段字典
| 字段名 | 类型 | 字段角色 | 是否核心字段 | 说明 |
|---|---|---|---|---|
| `logged_at` | timestamp | 时间 | 是 | 登录时间 |
| `owner_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `event_type` | string | 维度 | 否 | 事件属性字段，用于标识行为名称、事件分类或事件补充信息。 |
| `package_name` | string | 主键 | 是 | 包名 |
| `transaction_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `purchase_time` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `cancellation_time` | timestamp | 时间 | 是 | 取消时间 |
| `change_renewal_time` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `auto_renew_product_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `original_transaction_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `is_trial_period` | boolean | 维度 | 否 | 布尔标记字段，用于表示是/否状态。 |
| `is_in_intro_offer_period` | boolean | 维度 | 否 | 布尔标记字段，用于表示是/否状态。 |
| `cancellation_date_ms` | int | 指标 | 否 | 整型指标字段，用于统计数量、金额或次数。 |
| `price_amount_micros` | int | 指标 | 是 | 价格字段，用于记录商品、服务或订阅价格。 |
| `price_currency_code` | string | 维度 | 是 | 价格字段，用于记录商品、服务或订阅价格。 |
| `campaign` | string | 维度 | 否 | 投放campaign id |
| `install_time` | timestamp | 时间 | 是 | 安装时间 |
| `gclid` | string | 维度 | 否 | 一种用户的身份id |
| `logged_date` | date | 时间 | 是 | 登录日期 |
| `event_name` | string | 维度 | 是 | 事件名/事件英文名 |

## 4. 核心字段
| 字段名 | 类型 | 字段角色 | 说明 |
|---|---|---|---|
| `logged_at` | timestamp | 时间 | 登录时间 |
| `owner_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |
| `package_name` | string | 主键 | 包名 |
| `transaction_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |
| `purchase_time` | timestamp | 时间 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `cancellation_time` | timestamp | 时间 | 取消时间 |
| `change_renewal_time` | timestamp | 时间 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `auto_renew_product_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |
| `original_transaction_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |
| `price_amount_micros` | int | 指标 | 价格字段，用于记录商品、服务或订阅价格。 |
| `price_currency_code` | string | 维度 | 价格字段，用于记录商品、服务或订阅价格。 |
| `install_time` | timestamp | 时间 | 安装时间 |
| `logged_date` | date | 时间 | 登录日期 |
| `event_name` | string | 维度 | 事件名/事件英文名 |

## 5. 典型使用场景
- 用于追溯用户、订单、事件等明细事实，适合做深度诊断或二次聚合。

## 6. 查询风险与注意事项
- 飞书表信息中未提供额外查询限制；仍建议结合日期、项目和主键字段控制扫描范围。

## 7. 血缘和关联
| 项目 | 内容 |
|---|---|
| 上层表 | - |
| 是否需要展示血缘 | 是 |
| 数据来源 | 服务端 |
| 数据表地址 | - |
| 任务地址 | - |
| 看板 | - |

## 8. 待补充项
- 补充 `行粒度`。
- 补充 `主键字段`。
- 补充 `更新频率`。
