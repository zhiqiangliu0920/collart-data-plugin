---
doc_type: "table_knowledge"
source: "feishu_bitable"
generated_at: "2026-05-21T01:15:16+08:00"
table_name: "aidata2025.dwd.dwd_apple_user_subscribtion_di"
table_cn_name: "iOS账单粒度 账单表"
metadata_status: "complete"
field_status: "available"
projects: ["android"]
project_dirs: ["android"]
warehouse_space: "aidata"
layer: "DWD明细层"
subject: "收入成本主题"
secondary_subject: "账单粒度收入"
table_type: "事实表"
grain: "每个账单id一行"
primary_key: "transaction_id"
update_frequency: "T+1 12:00"
owner: "恬静"
priority: "中"
core_table: "是"
query_cost: "低"
source_record_ids: ["recvh8DkL6l3QO"]
field_count: "18"
---

# 表名：aidata2025.dwd.dwd_apple_user_subscribtion_di

> 表中文名：iOS账单粒度 账单表
> 生成来源：飞书 Bitable「数据知识库」；状态：`complete` / `available`。

## 1. 表定位
项目：android；分层：DWD明细层；表类型：事实表；主题：收入成本主题；二级主题：账单粒度收入；行粒度：每个账单id一行。

## 2. 基础信息
| 属性 | 值 |
|---|---|
| 表中文名 | iOS账单粒度 账单表 |
| 项目 | android |
| 数仓空间 | aidata |
| 分层 | DWD明细层 |
| 主题 | 收入成本主题 |
| 二级主题 | 账单粒度收入 |
| 表类型 | 事实表 |
| 行粒度 | 每个账单id一行 |
| 主键字段 | transaction_id |
| 核心表 | 是 |
| 更新频率 | T+1 12:00 |
| 维护人 | 恬静 |
| 使用优先级 | 中 |
| 查询成本 | 低 |
| 数据来源 | 苹果商店 / 服务端 / 客户端埋点 |

## 3. 字段字典
| 字段名 | 类型 | 字段角色 | 是否核心字段 | 说明 |
|---|---|---|---|---|
| `event_date` | date | 时间 | 是 | 日期 |
| `transaction_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `package_name` | string | 主键 | 是 | 包名 |
| `user_id` | string | 主键 | 是 | 用户id，内部平台分配的唯一id |
| `user_pseudo_id` | string | 主键 | 是 | 用户id，firebase分配的唯一id |
| `sku` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `purchase_date` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `original_purchase_date` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `expires_date` | timestamp | 时间 | 是 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `quantity` | int | 指标 | 否 | 整型指标字段，用于统计数量、金额或次数。 |
| `sub_type` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `transaction_type` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `storefront` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `country_code` | string | 维度 | 是 | 国家代码 |
| `country_name` | string | 维度 | 是 | 国家 |
| `price` | float | 指标 | 是 | 价格字段，用于记录商品、服务或订阅价格。 |
| `currency` | string | 维度 | 否 | 汇率 |
| `revenue_apple` | float | 指标 | 是 | 订阅收入 |

## 4. 核心字段
| 字段名 | 类型 | 字段角色 | 说明 |
|---|---|---|---|
| `event_date` | date | 时间 | 日期 |
| `transaction_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |
| `package_name` | string | 主键 | 包名 |
| `user_id` | string | 主键 | 用户id，内部平台分配的唯一id |
| `user_pseudo_id` | string | 主键 | 用户id，firebase分配的唯一id |
| `purchase_date` | timestamp | 时间 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `original_purchase_date` | timestamp | 时间 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `expires_date` | timestamp | 时间 | 用于记录事件发生、数据生成或分区时间的时间字段。 |
| `country_code` | string | 维度 | 国家代码 |
| `country_name` | string | 维度 | 国家 |
| `price` | float | 指标 | 价格字段，用于记录商品、服务或订阅价格。 |
| `revenue_apple` | float | 指标 | 订阅收入 |

## 5. 典型使用场景
- 用于追溯用户、订单、事件等明细事实，适合做深度诊断或二次聚合。

## 6. 查询风险与注意事项
- 存在 `event_date` 字段，按天查询时建议显式添加日期范围，避免全表扫描。

## 7. 血缘和关联
| 项目 | 内容 |
|---|---|
| 上层表 | pubdata2025.ods.ods_apple_sales_1d,aidata2025.dwd.dwd_oper_user_event_di,aidata2025.dwd.dwd_oper_user_basic_di,pubdata2025.ods.ods_woolong_subscription_1d,aidata2025.dwd.dwd_oper_user_transaction_di |
| 是否需要展示血缘 | 是 |
| 数据来源 | 苹果商店 / 服务端 / 客户端埋点 |
| 数据表地址 | [BigQuery](https://console.cloud.google.com/bigquery?project=aidata2025&ws=!1m5!1m4!4m3!1saidata2025!2sdwd!3sdwd_apple_user_subscribtion_di) |
| 任务地址 | [任务](https://console.cloud.google.com/bigquery/dataform/locations/us-east1/repositories/aidata/workspaces/script/files/definitions%2Fdwd%2Fdaily%2Fh_dwd_apple_user_subscribtion_di.sqlx?project=aidata2025) |
| 看板 | - |

## 8. 待补充项
- 暂无，后续可补充典型 SQL 和业务口径案例。
