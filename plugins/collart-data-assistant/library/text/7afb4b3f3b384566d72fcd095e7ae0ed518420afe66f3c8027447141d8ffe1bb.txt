# 表名：aidata2025.ads_collartfashion.ads_oper_user_revenue_di

> 文档整理：2026-09-12；业务状态：documented；复核：reviewed_static。原业务日期见正文，本次未重新查询生产数据。


## 1. 基础信息
- 表中文名：Fashion Stripe 订单收入
- 表类型：事实表
- 行粒度：1 笔 Stripe 订单（`transaction_id` 去重）
- 分区：`event_date`（`require_partition_filter = TRUE`）
- Dataform：`ads_oper_user_revenue_di`（`definitions/ads/collart/collart_fashion/ads_oper_user_revenue_di.sqlx`）
- 覆盖：2026-06-01 起

## 2. 核心作用
Fashion 付费订单明细。Stripe 字段原样同步，再用前后 10 分钟 `vip_subscribe_succeed` 判定是否归属 Fashion。

## 3. 口径
- 上游：`pubdata2025.dwd.dwd_cdct_revenue_stripe_di`（`app_name='collart_web'`，`revneue>0`）
- 归属：同一 `user_id`，|Stripe 时间 − 支付成功事件| ≤ 600 秒，取最近一条且 `package_name='collart_fashion'`
- 排除内部 `user_id`
- 金额字段名是上游的 `revneue`
- 用户日请 `SUM(revneue)` / `COUNT(*)`，不要读已停更的 `ads_collart.ads_oper_user_revenue_collart_fashion_di`
