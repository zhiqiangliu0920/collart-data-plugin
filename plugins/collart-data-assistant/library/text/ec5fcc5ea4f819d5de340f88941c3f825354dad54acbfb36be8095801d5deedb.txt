# aidata2025.ads.ads_collart_delivery_revenue_1d · collart ga、asa投放看板

> 业务状态：historical；复核：reviewed_static。以下完整字段定义来自 2026-05-21 导出，未经本次生产验证。

## 2026-05-21 字典依据

来源：飞书多维表格导出；生成时间 `2026-05-21T01:15:16+08:00`；记录标识 `recvh3C8YGRsHi`。这是原始元数据日期，不是当前生效日期。

- 原产品标记：['collart android']
- 原登记粒度：每天每个产品每个投放渠道每个国家一行
- 原登记主键：日期+包名+投放渠道+国家
- 更新频率：T+1 8:30
- 原登记层级：ADS应用层
- 主题：增长主题
- 子主题：投放回收
- 负责人：恬静

### 字段字典（原日期）

| 字段名 | 类型 | 字段角色 | 是否核心字段 | 说明 |
|---|---|---|---|---|
| `event_date` | date | 时间 | 是 | 日期 |
| `package_name` | string | 主键 | 是 | 包名 |
| `campaign_name` | string | 维度 | 否 | 投放campaign名称 |
| `country` | string | 维度 | 是 | 国家 |
| `cost` | float | 指标 | 是 | 投放花费 |
| `installs` | int | 指标 | 否 | 安装量 |
| `revenue_total` | float | 指标 | 是 | 收入 |
| `vip_revenue_total` | float | 指标 | 是 | 订阅收入 |
| `sub_revenue_aivideo` | float | 指标 | 是 | 直接订阅收入 |
| `pack_revenue_total` | float | 指标 | 是 | 点数包收入 |
| `ad_revenue_total` | float | 指标 | 是 | 广告收入 |
| `firebase_install_user` | int | 指标 | 是 | 整型指标字段，用于统计数量、金额或次数。 |
| `vip_user` | int | 指标 | 是 | 整型指标字段，用于统计数量、金额或次数。 |
| `revenue_0` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_1` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_2` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_3` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_4` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_5` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_6` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_7` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_14` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_21` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_30` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_45` | float | 指标 | 是 | 收入数值字段。 |
| `revenue_60` | float | 指标 | 是 | 收入数值字段。 |
| `dnu` | int | 指标 | 否 | 新增用户 |
| `retain_2` | int | 指标 | 否 | 整型指标字段，用于统计数量、金额或次数。 |

### 上游与来源

| 项目 | 内容 |
|---|---|
| 上层表 | aidata2025.dws.dws_oper_newuser_lifecycle_1d,aidata2025.ads_collart.ads_oper_basic_indicator_collart_di |
| 是否需要展示血缘 | 是 |
| 数据来源 | 服务端 / 投放后台 |
| 数据表地址 | [BigQuery](https://console.cloud.google.com/bigquery?project=aidata2025&ws=!1m5!1m4!4m3!1saidata2025!2sads!3sads_collart_delivery_revenue_1d) |
| 任务地址 | [任务](https://console.cloud.google.com/bigquery/dataform/locations/us-east1/repositories/aidata/workspaces/script/files/definitions%2Fads%2Fdaily%2Fh_ads_collart_delivery_revenue_1d.sqlx?project=aidata2025) |
| 看板 | collart ga、asa投放看板 |

### 使用限制

- retain_2 的历史说明为次日；其他 retain_N 的自动解释可能偏移一天，不能按字段后缀直接认定 Dn。
- 部分字段说明为导出模板的通用解释，不是已确认业务口径；金额单位、去重键、观察窗口仍需业务证据。
