# SQL 最佳实践与编码规范

> 文档整理：2026-09-12；业务状态：documented；复核：reviewed_static。原业务日期见正文，本次未重新查询生产数据。


> 记录时间：2026-03-27
> 来源：强强实战案例提炼
> 适用：BigQuery / 类似SQL引擎

---

## 一、成本优化原则

### 1.1 减少表扫描次数（核心原则）
**原则**：同一表需要统计多个事件/指标时，用 `IN` 一次性读取，再用 `CASE WHEN` 或 `COUNTIF` 分流

```sql
-- ✅ 推荐：一次扫描，分流统计（省成本）
SELECT
  COUNTIF(event_name = 'ai_service_start') AS start_cnt,
  COUNTIF(event_name = 'ai_service_success') AS success_cnt
FROM `project.dataset.events_*`
WHERE _TABLE_SUFFIX = '20260326'
  AND event_name IN ('ai_service_start', 'ai_service_success')

-- ❌ 避免：多次扫描同一表（费成本）
-- CTE1读一次events，CTE2再读一次events
```

**成本对比**：
| 写法 | 扫描次数 | 成本 |
|:---|:---|:---|
| 推荐写法 | 1次 | 低 ✅ |
| 多CTE分别读 | N次 | 高 N倍 ❌ |

---

## 二、代码简洁原则

### 2.1 "宽表先行"模式
**原则**：先在一个CTE里把需要的参数字段都提取成列，后续直接用，避免重复子查询

```sql
-- ✅ 推荐：先打宽
WITH base AS (
  SELECT
    event_date,
    event_name,
    (SELECT ep.value.string_value FROM UNNEST(event_params) ep WHERE ep.key = 'service_type') AS service_type,
    (SELECT ep.value.int_value FROM UNNEST(event_params) ep WHERE ep.key = 'time') AS success_time
  FROM `project.dataset.events_*`
  WHERE _TABLE_SUFFIX = '20260326'
    AND event_name IN ('ai_service_start', 'ai_service_success')
)
SELECT
  service_type,
  AVG(IF(event_name = 'ai_service_success', success_time, NULL)) AS avg_time
FROM base
GROUP BY ALL

-- ❌ 避免：每个聚合都写子查询
-- AVG((SELECT ep.value.int_value FROM UNNEST(event_params) ep WHERE ep.key = 'time'))
```

**优点**：
- 代码可读性高
- 执行效率更高（子查询只执行一次）
- 维护成本低

---

### 2.2 使用 COUNTIF 替代 CASE WHEN
**原则**：BigQuery 支持 `COUNTIF(condition)`，比 `COUNT(CASE WHEN ... END)` 更简洁

```sql
-- ✅ 推荐
COUNTIF(event_name = 'ai_service_start') AS start_pv

-- ❌ 避免（虽然功能一样，但冗长）
COUNT(CASE WHEN event_name = 'ai_service_start' THEN 1 END) AS start_pv
```

---

### 2.3 使用 GROUP BY ALL
**原则**：自动按所有非聚合字段分组，减少重复代码

```sql
-- ✅ 推荐
SELECT
  event_date,
  service_type,
  COUNT(*) AS cnt
FROM table
GROUP BY ALL  -- 自动按 event_date, service_type 分组

-- ❌ 避免
-- GROUP BY event_date, service_type  -- 字段多的时候容易漏
```

---

## 三、BigQuery 专用技巧

### 3.1 _TABLE_SUFFIX 分区过滤
**原则**：查询 events_* 表时必须使用 `_TABLE_SUFFIX` 过滤，禁止用 `event_date` 替代

```sql
-- ✅ 正确
WHERE _TABLE_SUFFIX BETWEEN '20260301' AND '20260307'

-- ❌ 错误（会导致全表扫描）
WHERE event_date BETWEEN '20260301' AND '20260307'
```

### 3.2 事件参数提取
**原则**：优先使用标量子查询，不要无脑全量 UNNEST

```sql
-- ✅ 推荐：标量子查询
(SELECT ep.value.string_value FROM UNNEST(event_params) ep WHERE ep.key = 'service_type') AS service_type

-- ❌ 避免：先UNNEST再JOIN（数据膨胀风险）
```

---

## 四、实战模板：多事件对比分析

**场景**：对比两个相关事件的指标（如启动vs成功）

```sql
WITH base AS (
  -- 第一步：宽表先行，提取所有需要的字段
  SELECT
    event_date,
    event_name,
    (SELECT ep.value.string_value FROM UNNEST(event_params) ep WHERE ep.key = 'service_type') AS service_type,
    (SELECT ep.value.int_value FROM UNNEST(event_params) ep WHERE ep.key = 'time') AS success_time
  FROM `storytemplate-10a27.analytics_232977577.events_*`
  WHERE _TABLE_SUFFIX = '20260326'
    AND app_info.id IS NULL
    AND event_name IN ('ai_service_start', 'ai_service_success')
)

SELECT
  event_date,
  service_type,
  -- 事件计数
  COUNTIF(event_name = 'ai_service_start') AS start_pv,
  COUNTIF(event_name = 'ai_service_success') AS success_pv,
  -- 成功率（注意NULLIF防除0）
  ROUND(
    COUNTIF(event_name = 'ai_service_success') * 100.0 
    / NULLIF(COUNTIF(event_name = 'ai_service_start'), 0), 
    2
  ) AS success_rate_pct,
  -- 平均耗时（只在success事件有值）
  ROUND(
    AVG(IF(event_name = 'ai_service_success', success_time, NULL)) / 1000.0, 
    2
  ) AS avg_time_sec
FROM base
GROUP BY ALL
ORDER BY start_pv DESC
```

---

## 五、Checklist

写SQL前自查：
- [ ] 是否需要从同一表读多次？→ 考虑用 `IN` + `CASE WHEN/COUNTIF` 合并
- [ ] 是否重复提取event_params？→ 考虑"宽表先行"
- [ ] 分组字段多？→ 考虑用 `GROUP BY ALL`
- [ ] 除法计算？→ 记得用 `NULLIF(..., 0)` 防除0错误
- [ ] 查询events表？→ 必须使用 `_TABLE_SUFFIX` 分区过滤
- [ ] Web端数据？→ 必须加 `app_info.id IS NULL`

---

## 六、版本记录

| 日期 | 更新内容 | 来源 |
|:---|:---|:---|
| 2026-03-27 | 初版：成本优化、宽表先行、COUNTIF、GROUP BY ALL、_TABLE_SUFFIX | 强强实战案例 |

## 条件聚合与去重的共用约定

原 `sql-best-practices.md` 的 2026-03-25 示例强调 `COUNT(DISTINCT IF(condition, user_id, NULL))` 或等价 CASE 写法计算条件 UV；`COUNTIF(condition)` 计算满足条件的行数，不替代 UV。分母为 0 时用 SAFE_DIVIDE 或 NULLIF，并解释缺失值。

合并 CTE、参数提取或扫描并不保证固定倍数的性能收益，是否物化与分区裁剪须以执行计划/扫描量验证。旧学习摘记保留在历史目录，不能据其中“免费额度”“加速倍数”直接制定当前配置。
