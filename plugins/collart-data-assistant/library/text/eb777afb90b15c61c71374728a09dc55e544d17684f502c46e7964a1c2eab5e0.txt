---
doc_type: "table_knowledge"
source: "feishu_bitable"
generated_at: "2026-05-21T01:15:16+08:00"
table_name: "pubdata2025.dwd.dwd_cdct_revenue_stripe_di"
table_cn_name: "collart web收入表"
metadata_status: "complete"
field_status: "available"
projects: ["collart web"]
project_dirs: ["collart_web"]
warehouse_space: "pubdata"
layer: "DWD明细层"
subject: "收入成本主题"
secondary_subject: "web订阅收入"
table_type: "事实表"
grain: ""
primary_key: ""
update_frequency: ""
owner: "恬静"
priority: ""
core_table: "是"
query_cost: ""
source_record_ids: ["recvh3C8YG8nki"]
field_count: "23"
---

# 表名：pubdata2025.dwd.dwd_cdct_revenue_stripe_di

> 表中文名：collart web收入表
> 生成来源：飞书 Bitable「数据知识库」；状态：`complete` / `available`。

## 1. 表定位
项目：collart web；分层：DWD明细层；表类型：事实表；主题：收入成本主题；二级主题：web订阅收入。

## 2. 基础信息
| 属性 | 值 |
|---|---|
| 表中文名 | collart web收入表 |
| 项目 | collart web |
| 数仓空间 | pubdata |
| 分层 | DWD明细层 |
| 主题 | 收入成本主题 |
| 二级主题 | web订阅收入 |
| 表类型 | 事实表 |
| 行粒度 | - |
| 主键字段 | - |
| 核心表 | 是 |
| 更新频率 | - |
| 维护人 | 恬静 |
| 使用优先级 | - |
| 查询成本 | - |
| 数据来源 | 第三方支付平台（stripe等） |

## 3. 字段字典
| 字段名 | 类型 | 字段角色 | 是否核心字段 | 说明 |
|---|---|---|---|---|
| `event_date` | date | 时间 | 是 | 日期 |
| `event_timestamp` | int | 时间 | 是 | 事件触发时间 |
| `transaction_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `app_name` | string | 维度 | 是 | 产品名 |
| `charge_type` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `country` | string | 维度 | 是 | 国家 |
| `currency` | string | 维度 | 否 | 汇率 |
| `stripe_customer_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `amount` | float | 指标 | 是 | 交易金额 |
| `fee` | float | 指标 | 否 | 浮点型指标字段，用于统计金额、比率或均值。 |
| `revneue` | float | 指标 | 否 | 浮点型指标字段，用于统计金额、比率或均值。 |
| `description_type` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `order_status` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `product_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `product_name` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `subscription_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `subscription_status` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |
| `current_period_start` | int | 指标 | 否 | 订阅开始日期 |
| `current_period_end` | int | 指标 | 否 | 订阅结束日期 |
| `cancel_at_period_end` | boolean | 维度 | 否 | 布尔标记字段，用于表示是/否状态。 |
| `sub_price_id` | string | 主键 | 是 | 用于唯一标识记录或业务实体的关键字段。 |
| `user_id` | string | 主键 | 是 | 用户id，内部平台分配的唯一id |
| `payment_channel` | string | 维度 | 否 | 文本属性字段，用于补充业务描述、分类或标签信息。 |

## 4. 核心字段
| 字段名 | 类型 | 字段角色 | 说明 |
|---|---|---|---|
| `event_date` | date | 时间 | 日期 |
| `event_timestamp` | int | 时间 | 事件触发时间 |
| `transaction_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |
| `app_name` | string | 维度 | 产品名 |
| `country` | string | 维度 | 国家 |
| `stripe_customer_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |
| `amount` | float | 指标 | 交易金额 |
| `product_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |
| `subscription_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |
| `sub_price_id` | string | 主键 | 用于唯一标识记录或业务实体的关键字段。 |
| `user_id` | string | 主键 | 用户id，内部平台分配的唯一id |

## 5. 典型使用场景
- 用于追溯用户、订单、事件等明细事实，适合做深度诊断或二次聚合。

## 6. 查询风险与注意事项
- 存在 `event_date` 字段，按天查询时建议显式添加日期范围，避免全表扫描。

## 7. 血缘和关联
| 项目 | 内容 |
|---|---|
| 上层表 | - |
| 是否需要展示血缘 | 是 |
| 数据来源 | 第三方支付平台（stripe等） |
| 数据表地址 | [BigQuery](https://console.cloud.google.com/bigquery?project=pubdata2025&ws=!1m5!1m4!4m3!1spubdata2025!2sdwd!3sdwd_cdct_revenue_stripe_di) |
| 任务地址 | - |
| 看板 | - |

## 8. 待补充项
- 补充 `行粒度`。
- 补充 `主键字段`。
- 补充 `更新频率`。
